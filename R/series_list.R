#' List of All Possible Series That Can Be Extracted via the `series` Function
#'
#' This list is intended as a helper when using autocompletion during calls
#' to `series` (see examples).
#'
#' @examples
#' m <- seas(AirPassengers)
#' series(m, series_list$seats.seasonal)
"series_list"
